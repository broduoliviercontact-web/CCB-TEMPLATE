# Project Profile — React Web

For likely React and TypeScript web applications. It adds audit and testing contracts, not a
framework, dependencies or a claim that the stack was detected. Conventions cover components,
accessibility, loading states and testable UI behaviour.
