# Project Profile — Node API

For Node.js services and APIs. It adds audit and API-testing guidance, not an HTTP framework,
dependencies or inferred architecture. Conventions favour explicit contracts and error handling.
