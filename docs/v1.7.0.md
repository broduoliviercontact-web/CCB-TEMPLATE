# V1.7.0 — Declarative agents

V1.7.0-A adds declarative project roles through `.ccb/agents.conf` and the `agents`, `agent show`, and `agent validate` commands. Access is explicitly declarative only; no agent execution, sandbox, permission mutation, provider call, or network access is part of this release.
